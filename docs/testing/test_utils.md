## Test utils

Contains methods that can be used within unit tests.

Included request methods:
* GET
* POST

The above request methods are mocked using the Django testing client. See:

`https://docs.djangoproject.com/en/3.1/topics/testing/tools/`
